# DDC single-leg-balance benchmark — evaluation dependencies.
# The policy runs on CPU (numpy); a CPU-only torch build is sufficient.
# Versions in parentheses are what the release was validated against.
numpy>=1.24        # (tested 1.26.4)
mujoco>=3.2        # (tested 3.10.0)
torch>=2.1         # (tested 2.12.1; used only to load the .pt state_dict — CPU build is fine)

# The headline benchmark (run_eval.py) needs nothing below — it scores model_0262000.pt directly from
# the .pt via fast_policy.py. onnx + onnxruntime are needed for eval/export_onnx.py (exports the actor
# ONNX) and for the 8 baseline adapters (benchmark/baselines/, see SETUP.md); install both if you use either:
# onnx>=1.14         # (tested 1.17)
# onnxruntime>=1.16  # (tested 1.23.2)
